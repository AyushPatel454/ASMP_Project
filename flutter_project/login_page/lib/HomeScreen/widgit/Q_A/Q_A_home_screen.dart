import 'package:flutter/material.dart';
class que_ans extends StatefulWidget {
  const que_ans({Key? key}) : super(key: key);

  @override
  State<que_ans> createState() => _que_ansState();
}

class _que_ansState extends State<que_ans> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this is question and answer page'),
    );
  }
}
