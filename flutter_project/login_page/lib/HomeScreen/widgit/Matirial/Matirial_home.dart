import 'package:flutter/material.dart';
class matirial extends StatelessWidget {
  const matirial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('this is matirial'),
    );
  }
}